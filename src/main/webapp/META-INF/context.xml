<?xml version="1.0" encoding="UTF-8"?>
<Context>
    <!-- 세션 쿠키에 SameSite 속성 부여(크로스사이트 요청 시 쿠키 미전송 → CSRF 완화).
         lax는 최신 브라우저 기본값이며 사이트 내 일반 내비게이션(로그인 후 리다이렉트 등)을 깨지 않는다.
         더 강하게 막으려면 strict로 변경(외부 링크 진입 시 재인증처럼 보일 수 있음). -->
    <CookieProcessor sameSiteCookies="lax" />
</Context>
